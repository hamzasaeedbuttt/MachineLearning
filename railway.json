{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn api.main:app --host 192.168.0.202:8000 --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}

